import { ArrowLeft, ArrowUpRight, Check } from 'lucide-react';
import { modules } from '../data';
import Copilot from '../components/Copilot';
import { Badge } from '../components/UI';
export default function ModulePage({id}:{id:string}){const module=modules.find(m=>m.id===id);if(!module)return <section className="panel module-page"><h1>Page introuvable</h1><a href="#/">Revenir à l’accueil</a></section>;return <><a className="back-link" href="#/"><ArrowLeft size={16}/>Tableau de bord</a><div className="page-heading"><div><p className="eyebrow">LES ESPACES CFE</p><h1>{module.label}</h1><p>{module.description}</p></div><module.icon size={42} className="text-magenta"/></div><section className="panel module-page"><Badge tone="info">{id==='copilote'?'Simulation disponible':'Module à venir'}</Badge><h2>Un espace pensé pour votre métier.</h2><p>Le socle de navigation est prêt. Les parcours suivants seront développés lors des prochains jalons :</p><ul>{module.features.map(f=><li key={f}><Check size={18}/>{f}</li>)}</ul><p className="small">Cette première version ne se connecte à aucun service métier et ne traite aucune donnée réelle.</p><a href="#/">Retour à vos actions <ArrowUpRight size={16}/></a></section>{id==='copilote'&&<div className="module-copilot"><Copilot/></div>}</>}
