import { useState } from 'react';
import { Sparkles, ArrowUp, ArrowUpRight } from 'lucide-react';
import { Button } from './UI';
export default function Copilot(){const [question,setQuestion]=useState('');const [answer,setAnswer]=useState('');
function respond(value:string){setQuestion(value);setAnswer('Exemple de méthode : précisez votre objectif, identifiez le public concerné, puis préparez un brouillon à faire valider. Le service IA et les sources CFE ne sont pas encore connectés. Aucune réponse métier n’est générée dans cette démonstration.');}
return <section className="copilot panel" aria-labelledby="copilot-title"><div className="copilot-heading"><span className="icon-tile"><Sparkles size={22}/></span><span className="eyebrow">À VOS CÔTÉS</span><span className="demo-tag">Démo</span></div><h2 id="copilot-title">Un coup de main<br/>pour aller plus loin.</h2><p>Préparer, explorer, rédiger.<br/>Votre Copilote vous accompagne.</p><button className="suggestion" onClick={()=>respond('Préparer une communication')}>Préparer une communication <ArrowUpRight size={16}/></button><button className="suggestion" onClick={()=>respond('Organiser une formation')}>Organiser une formation <ArrowUpRight size={16}/></button><form onSubmit={e=>{e.preventDefault();if(question.trim())respond(question)}}><label className="sr-only" htmlFor="copilot-question">Votre question au Copilote</label><div className="prompt-input"><input id="copilot-question" value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Une idée, une question ?" maxLength={500}/><Button aria-label="Envoyer la question" disabled={!question.trim()} type="submit"><ArrowUp size={18}/></Button></div></form><p className="small">Simulation locale · aucune donnée transmise</p><div role="status">{answer&&<p className="copilot-answer">{answer}</p>}</div></section>}
